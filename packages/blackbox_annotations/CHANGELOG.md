## Next
- Breaking: removed `@observable`; output tracking now comes from core hooks and UI runtimes.
- Breaking: `@persistent` no longer accepts a `store` type; persistent boxes use the global `BlackboxPersistence` store.

## 0.0.2
- Initial release
