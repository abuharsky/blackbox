name: blackbox
description: >
  Deterministic reactive computation core with explicit dependency graphs,
  boxes, and flows. Designed for testable business logic and state pipelines.
repository: https://github.com/abuharsky/blackbox/tree/main/packages/blackbox_core
version: 0.0.5
license: MIT
topics:
  - reactive
  - state-management
  - data-flow
  - dependency-graph
  - computation-graph

environment:
  sdk: ">=3.5.0 <4.0.0"
resolution: workspace

dependencies:
  meta: ^1.17.0

dev_dependencies:
  test: ^1.25.0
