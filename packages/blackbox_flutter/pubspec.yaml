name: blackbox_flutter
description: Flutter bindings for Blackbox (BoxObserver + BoxProvider + persistence adapters).
version: 0.0.4
repository: https://github.com/abuharsky/blackbox/tree/main/packages/blackbox_flutter
environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.19.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.4

  blackbox: ^0.1.0
  blackbox_support: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
